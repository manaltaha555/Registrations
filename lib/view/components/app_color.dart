import 'package:flutter/material.dart';

class AppColor {
  static Color surface = Color(0XFF18191D);
  static Color primary = Color(0xff23C2ED);
  static Color subtitle = Colors.white.withValues(alpha: 0.5);
  static Color border = Colors.white.withValues(alpha: 0.08);
  static Color fillColor = Colors.white.withValues(alpha: 0.04); 
  static Color title = Colors.white;
}
